# sequencemap

`sequencemap` is an R package scaffold for sequence mapping, phylogenetic
analysis, and spatial epidemiology workflows.

## Development

Install package dependencies, then regenerate documentation with `roxygen2`
and run tests with `testthat`.
